{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword.first) }}
    {{ form_row(form.plainPassword.second) }}
    {{ form_row(form.termsAccepted, {'label' : 'I accept the <a href="#">Terms and Conditions</a>'}) }}

    <button type="submit">Register!</button>
{{ form_end(form) }}